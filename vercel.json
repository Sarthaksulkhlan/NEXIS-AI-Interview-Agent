{
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "functions": {
    "api/[...path].cjs": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
